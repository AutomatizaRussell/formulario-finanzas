version: '3.8'

services:
  backend-operaciones-prof:
    build: ./backend
    restart: unless-stopped
    environment:
      - PORT=3000
    networks:
      - net_vinculados

  frontend-operaciones-prof:
    image: nginx:alpine
    restart: unless-stopped
    volumes:
      - ./frontend:/usr/share/nginx/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - net_vinculados
      - red_pro

networks:
  net_vinculados:
    driver: bridge
  red_pro:
    external: true